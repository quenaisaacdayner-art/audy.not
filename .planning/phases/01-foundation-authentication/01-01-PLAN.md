---
phase: 01-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tailwind.config.ts
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/lib/utils.ts
  - components.json
  - src/components/ui/button.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/form.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - .env.local.example
autonomous: true

must_haves:
  truths:
    - "Next.js 15 app runs with npm run dev"
    - "Tailwind CSS styles are applied"
    - "shadcn/ui components are available for import"
    - "TypeScript compiles without errors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "next"
    - path: "src/app/layout.tsx"
      provides: "Root layout"
      min_lines: 10
    - path: "src/components/ui/button.tsx"
      provides: "shadcn Button component"
      exports: ["Button"]
    - path: "src/components/ui/dialog.tsx"
      provides: "shadcn Dialog component"
      exports: ["Dialog"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
---

<objective>
Scaffold the Next.js 15 project with TypeScript, Tailwind CSS 4, and shadcn/ui base components.

Purpose: Create the foundation that all subsequent plans build upon. Without a working project scaffold, no other development can proceed.

Output: A runnable Next.js application with all core dependencies installed and shadcn/ui components initialized.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 15 project with dependencies</name>
  <files>
    package.json
    tsconfig.json
    tailwind.config.ts
    next.config.ts
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
    src/lib/utils.ts
    .env.local.example
  </files>
  <action>
    Create Next.js 15 project using create-next-app with these options:
    - TypeScript: Yes
    - Tailwind CSS: Yes
    - App Router: Yes
    - src/ directory: Yes
    - Import alias: @/*

    Run: `npx create-next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*" --yes`

    After creation, install additional dependencies:
    ```bash
    npm install @supabase/supabase-js @supabase/ssr
    npm install zod react-hook-form @hookform/resolvers
    npm install server-only client-only
    ```

    Create .env.local.example with required environment variables:
    ```
    NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
    NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
    NEXT_PUBLIC_SITE_URL=http://localhost:3000
    ```

    Update src/app/page.tsx to be a minimal placeholder (remove default Next.js boilerplate):
    ```tsx
    export default function Home() {
      return (
        <main className="flex min-h-screen items-center justify-center">
          <h1 className="text-4xl font-bold">Audy.not</h1>
        </main>
      )
    }
    ```
  </action>
  <verify>
    Run `npm run dev` and confirm app starts on http://localhost:3000
    Run `npm run build` and confirm no TypeScript errors
  </verify>
  <done>
    Next.js 15 project created with TypeScript, Tailwind, App Router, and all required dependencies installed. Dev server runs without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui with required components</name>
  <files>
    components.json
    src/components/ui/button.tsx
    src/components/ui/dialog.tsx
    src/components/ui/form.tsx
    src/components/ui/input.tsx
    src/components/ui/label.tsx
    src/lib/utils.ts
  </files>
  <action>
    Initialize shadcn/ui:
    ```bash
    npx shadcn@latest init
    ```

    When prompted, select:
    - Style: New York
    - Base color: Neutral
    - CSS variables: Yes

    Add required components for auth forms:
    ```bash
    npx shadcn@latest add button dialog form input label
    ```

    Verify src/lib/utils.ts exists and contains the `cn` utility function for className merging.

    Note: shadcn/ui will update tailwind.config.ts and globals.css with its configuration. This is expected.
  </action>
  <verify>
    Confirm components.json exists in project root
    Confirm src/components/ui/ contains button.tsx, dialog.tsx, form.tsx, input.tsx, label.tsx
    Import Button in src/app/page.tsx and render it to verify component works:
    ```tsx
    import { Button } from "@/components/ui/button"

    export default function Home() {
      return (
        <main className="flex min-h-screen items-center justify-center">
          <Button>Test Button</Button>
        </main>
      )
    }
    ```
    Run `npm run dev` and confirm button renders with styling
  </verify>
  <done>
    shadcn/ui initialized with button, dialog, form, input, and label components. Components are importable from @/components/ui/* and render correctly with Tailwind styling.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. `npm run build` completes without TypeScript errors
3. http://localhost:3000 shows styled content
4. shadcn Button component renders with correct styling
5. All dependencies in package.json: next, @supabase/supabase-js, @supabase/ssr, zod, react-hook-form
</verification>

<success_criteria>
- Next.js 15 project runs successfully
- TypeScript compilation passes
- Tailwind CSS applies styles
- shadcn/ui components (button, dialog, form, input, label) are available
- Supabase and validation packages are installed
- .env.local.example documents required environment variables
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-01-SUMMARY.md`
</output>
